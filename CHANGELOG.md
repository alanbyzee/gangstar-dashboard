# 看板操作记录（CHANGELOG）

> 本文件记录 Gangstar 运营看板的所有**数据变更与结构操作**。
> 每次增删改都追加一条记录，并对应一次 git 版本提交 —— 任何被删的内容都能精确找回，更新其他数据时不会丢。
>

### 2026-08-04 ｜ 「延期 / Delay」标记优化：移到卡片右上角「划」旁 + 整卡高亮（用户反馈修正）
- 用户反馈：不要底部状态栏形式，要放在每个格子上方「划」按钮旁边；点了之后**整张卡片**标记为 delay，而非只在小栏写 delay。
- 改动：①「延」按钮从卡片底部状态栏移到**右上角操作行、紧挨「划」按钮**（.sa-post 用 `.act.delay` / .item 用 `.iact.delay`，data-pid / data-uid 绑定）；② 点亮后 `.delayed` 改为**整卡高亮**：1.5px 琥珀全边框 + 琥珀淡底 + 外发光，不再只是左边框；按钮本身变实心琥珀「延」。
- 存储/同步/撤销不变：仍用 `gangstar_delayed` 键，SYNC_KEYS（实时同步）+ UNDO_KEYS（全局撤销）覆盖。
- 无头测试 PASS：两表「延」按钮均在「划」旁；点击后整卡 border=1px rgb(232,151,31)、bg 琥珀、wholeDelayed=true；无残留 .delay-box；切到「区域素材」Tab 正常渲染 26 个素材各带「延」按钮；Firebase 拦截零污染。

### 2026-08-04 ｜ 新增「延期 / Delay」素材状态标记（初版，已被上方优化取代）
- 初版在每个排期素材卡片底部加琥珀色「延期」开关；后按用户反馈改为右上角「划」旁 + 整卡高亮（见上条）。

### 2026-08-04 ｜ 修复添加素材弹窗卡顿+从左边偏移弹出 ⚠️ 性能/视觉修复
- **现象**：点击"添加素材"页面卡顿，弹窗从页面左边卡顿一下才出来。
- **根因**：`.sa-modal-bg`（全屏遮罩层）错误复用了 `.sa-toast` 的 `sa-fade` 关键帧，该 keyframes 含 `translateX(-50%)`——对 `inset:0` 铺满层等于把整个弹窗容器左移半个屏幕，配合 `backdrop-filter:blur` 合成导致掉帧/卡顿感。
- **修复**：新建纯透明度淡入关键帧 `@keyframes sa-bg-in{from{opacity:0}to{opacity:1}}`，遮罩层改用此动画（无位移）；弹窗本身保留 `sa-pop`（translateY -4px 微上浮）正常进入。实测打开延迟 23ms、弹窗完美居中。

### 2026-08-04 ｜ 修复 hashtag 无法一次性添加（重构为「已选」行模型）⚠️ 交互修复
- **现象**：新增内容时添加 hashtag 无法一次成功，添加完还要再手动点一下这个 hashtag 才能加上。
- **根因**：自定义标签回车后会创建一个**已处于选中态(ht-active)** 的 chip，但点击 handler 对 chip 用 `classList.toggle('ht-active')` —— 用户出于习惯去「点一下确认」，反而把已选中的 chip 取消掉了，导致保存时 `collectHashtags()` 取到空数组。预设标签虽能一次选中，但整体交互不一致、易误触。
- **修复（重构选择器，两文件同步）**：
  1. `buildHashtagPicker` 新增独立「**已选 Hashtag**」行(`#ht-selected`) 作为唯一数据源；预设/最近标签池不再预置 active 态。
  2. 点击预设/最近标签 → 加入「已选」行；输入回车 → 加入「已选」行（不再需要再点）。
  3. 移除方式：点「已选」行里的标签本身，或点其右上角 `×`。
  4. `collectHashtags()` 改为只读 `#ht-selected .ht-chip`；新增 `toggleHashtagSelected(bg, tag)` 去重辅助函数；3 个弹窗(添加/编辑/区域)handler 同步更新。
- **验证**：无头测试（拦截 Firebase 防污染）通过 —— 点预设+回车一次性得到 `["#Gangstar","#mytag1"]`；点已选标签 / 点 × 均可移除；无页面报错。

### 2026-08-04 ｜ 修复点击卡片内元素（已提需求等）自动收起
- **现象**：点击卡片内的「已提需求」checkbox 或其他元素时，卡片/浮层自动收起消失。
- **根因（两个）**：
  1. 区域素材周历的 `.req-box` 点击 handler 缺少 `e.stopPropagation()`，事件冒泡到父级触发不期望的行为。
  2. 同步引擎的 `rerender()` 在云端变更到达时会整个重建 DOM，已打开的详情 popover / 编辑弹窗被静默销毁且无提示。
- **修复**：区域 req-box 补 stopPropagation；`rerender()` 增加浮层感知（重渲染前检测打开的 popover/modal，渲染后 toast 提示用户）。

### 2026-08-04 ｜ 同步引擎重写（防丢 + 逐键实时 + 云端备份）⚠️ 重要架构修复
- **背景**：此前 `applyRemote` 在云端某键缺失/为 null 时会**删除本地同名键**，且 `push()` 用整 blob 覆盖。一次测试/异常写入把空状态覆盖上云端 → 所有设备的真实数据（KOC、提需求等）被整场清空，且**无任何备份可恢复**。
- **重写核心（两文件同步）**：
  1. **逐键写入**：`fbRef.child(k).set(value)` 只改自己的键，绝不再整 blob 覆盖其他键（F2 修复）。
  2. **缺键不再删本地**：`applyRemote` 对云端缺失的键 `continue`（保留本地），只有显式 `__GST_DEL__` 哨兵才删除 → 杜绝"空库清全场"（F1 根治，正是之前的丢数据根因）。
  3. **删除可传播**：`removeItem` 写 `__GST_DEL__` 哨兵，其他设备 applyRemote 收到后本地删除；不再"只增不删"。
  4. **追加式云端备份**：每次变更写时间戳快照到 `gangstar/backups/<ts>`（保留最近 60 条）；另每 5 分钟 + `beforeunload` 自动备份。
  5. **备份恢复面板**：右下角新增「🕘 备份」按钮，列出最近 30 条备份、可一键恢复该时间点数据到云端并同步全员。
  6. **旧格式迁移**：`migrateOld()` 首次加载把旧 `{data:{...}}` 结构逐键迁移到新结构，保留既有关键（如 gangstar_cancelled）。
  7. 自动保存静音（仅在手动「⇧ 上传本地改动」时提示）；`gangstar_undo` 仍仅本地、不同步。
- **校验**：离线内存 mock Firebase（拦截全部真实请求、零污染）无头测试两文件 PASS —— 逐键写入 / 缺键保留本地(F1) / 删除哨兵传播 / 备份快照生成 / 零报错；公网已强制重建上线（`BACKUP_PATH`=4）
- 主看板 + 分享版均已 git commit

### 2026-08-04 ｜ 修复 hashtag 选择器「点不动 / 回车无反应」
- **现象**：用户在添加/编辑素材弹窗里无法使用 hashtag（点了没反应、保存后没记录）
- **根因 1**：点击监听只绑在 `#ht-pool`，而「最近使用（7天内）」区的 chip 在 `#ht-recent` 容器里 → 那一栏的 tag 完全点不动
- **根因 2**：自定义输入框 `#ht-custom` 没有 keydown 监听 → 打完标签回车毫无反馈（保存时会读 value，但 UI 无反应、看不到标签被加入）
- **修复**：① 把点击切换监听的范围从 `#ht-pool` 扩大到整个 `.ht-row`（同时覆盖 预设区 + 最近使用区）；② 给 `#ht-custom` 加回车监听，回车即时把输入变成一个 `ht-chip ht-active` 加入 `#ht-pool`（自动补 `#` 前缀、去重）；③ 三个弹窗（添加素材 / 编辑素材 / 区域编辑）统一替换
- **校验**：无头浏览器测试 PASS —— 最近使用区点击切换生效、自定义回车生成 chip、保存后 hashtags 完整写入 localStorage、7天历史记录成功；公网已强制重建上线（`.ht-row` 监听 = 3）
- 主看板 + 分享版均已 git commit

### 2026-08-04 ｜ 修复分享版区域素材周历 hashtag 渲染崩溃（+ 收尾 hashtag 选项）
- **现象**：分享版「区域素材周历」一栏整体报错/不渲染，hashtag 选项虽在添加弹窗里但区域卡片不显示已选标签
- **根因**：区域周历渲染循环里使用了 `htHtml` 变量（显示已选 hashtag），但该作用域从未声明它（只取出了 `hashtags` 数组却没转成 `htHtml`）→ ReferenceError 中断整块渲染。主看板此前已补好，分享版漏了这一行
- **修复**：区域周历单元格渲染补上 `const htHtml=(hashtags&&hashtags.length)?...:''`（与主看板一致）。**其他所有更新原样保留**：添加/编辑素材弹窗的 hashtag 选择器、本周涨粉、Firebase 实时同步等均未改动
- **校验**：提取内联 JS 语法检查 PASS（SYNTAX_OK）；部署 GitHub Pages 后公网已确认上线（raw 源 + Pages 均含 HASHTAG_PRESETS 代码）
- 主看板 + 分享版均已 git commit

### 2026-08-04 ｜ 新增 hashtag「7天最近使用」记忆（多端同步）
- **需求**：用过的 hashtag（尤其自定义输入）在接下来 7 天内保留，在添加/编辑素材弹窗顶部「最近使用」区直接点选，免重复手打
- **实现**：新增 `recordHashtagUsage(tags)` —— 每次保存素材时把用过的 tag（带时间戳）写入 `gangstar_hashtag_history`；`buildHashtagPicker` 读取并按时间倒序在顶部渲染「最近使用（7天内）」区；`collectHashtags` 同时收集预设池(#ht-pool)与最近使用池(#ht-recent)
- **7天过期**：写入和读取时都按 `now-7d` 过滤，超期自动剔除
- **多端同步**：将 `gangstar_hashtag_history` 加入 Firebase 同步键 `SYNC_KEYS`（经现有 setItem 包装自动上云、多端秒级同步）；**未**加入 `UNDO_KEYS`，避免 ⌘Z 撤销素材时误伤常用 tag 历史
- **样式**：新增 `.ht-recent`（浅紫虚线框）+ `.ht-recent-label`（紫色小标题）
- **校验**：两文件 JS 语法 PASS；无头浏览器测试 PASS（函数已定义 / 最近使用区渲染 / 记录写入与合并 / 7天过期过滤均正确）；已部署 GitHub Pages 公网生效（公网 HTML 含 gangstar_hashtag_history 代码）
- 主看板 + 分享版同步、已 git commit

### 2026-08-03 ｜ 看板接入 Firebase 实时同步（所有编辑状态多端秒级同步）
- **背景**：此前「同步」只推送了排期源文件(hq_data.js)和粉丝(fan_data.json)；用户真正手改的东西（链接/排序/描述/隐藏划线/确认格子/自定义素材等）全在各自浏览器 localStorage，从不上传 → 多端不同步
- **方案**：接入 Firebase Realtime Database（项目 `gstmin`，路径 `gangstar/state`）
- **实现**：`localStorage.setItem/removeItem` 包装，检测 `UNDO_KEYS`（10 个状态键）变更即 debounce(400ms) 写入 Firebase；`on('value')` 实时订阅，任意端改动所有打开页面秒级自动重渲染
- **防回声**：每次写入带 `writer` 标记，自身回声跳过；离线/SDK 加载失败优雅降级为本地模式
- **迁移种子**：右下角新增「⇧ 上传本地改动」按钮，首次把当前本地编辑一次性推上云端
- **UI**：右下角徽章「● 实时同步中 / ○ 仅本地·未同步」
- 双文件（主看板+分享版）同步接入；分享版已重部署 GitHub Pages（commit 6c88017）
- 无头双端测试 PASS：写入云端 OK / A↔B 双向实时同步 OK / 零报错 / 测试数据已清理

### 2026-08-03 ｜ 修复编辑素材时回车自动退出页面
- **现象**：编辑素材（标题/链接）时一回车就自动保存退回到页面，来不及填其他字段
- **根因**：三个编辑弹窗（添加/编辑南亚/编辑区域）的标题框绑了 `onkeydown` Enter→`saveFn()`，回车即保存关闭
- **修复**：移除回车保存逻辑，弹窗仅能通过「保存」/「取消」按钮关闭，回车不再触发任何关闭
- 无头测试 PASS：回车保持打开 / 点保存才关；两文件同步、已 git commit

### 2026-08-03 ｜ 汇总卡片增加「本周涨粉」标记 + IG/FB 数据更新
- **粉丝数据更新（截图）**：IG 2,568 → **2,791**（+223）；FB 46 → **48**（+2）；总计 4,083
- **本周涨粉标记**：每平台汇总卡片新增绿色「本周 ▲+N」标签，基于 `weeklyGrowth()` 计算（找 ~7 天前锚点）
  - Instagram 本周 ▲+1,109 / YouTube 本周 ▲+848 / Facebook 本周 ▲+9 / X 本周 ▲+1
  - 全渠道本周总涨 ▲+1,967（紫色渐变徽章显示在总粉丝卡片上）
- **汇总卡片扩展**：从 4 张增至 6 张（新增 Facebook、X(Twitter) 独立卡片），每张都带周涨标签
- 新增 CSS 类：`.wk-up`（绿）/ `.wk-down`（红）/ `.wk-flat`（灰）/ `.wk-total`（紫渐变）

### 2026-08-02 ｜ UI 密度优化（降低拥挤感）
- **操作按钮默认隐藏**：opacity:.35 几乎透明，hover 卡片时才完全显示（✎划×⠿ 不再一直抢视觉）
- **卡片间距加大**：margin 5→7px、padding +3px、格子 min-height +10px
- **文字层级**：标题加粗加大(12.5px/600)、描述行高 1.45、标签 inline 不换行
- **次要信息收敛**：partner/req-box 字号缩小+颜色更淡、添加按钮 16→14px
- **交互**：卡片 hover 微上浮(-1px) + 阴影加深
- 27 条 CSS 规则替换，主看板+分享版同步

### 2026-08-02 ｜ 全局视觉美化
- **范围**：CSS 全面重写（JS 未动），主看板 + 分享版同步
- **背景**：微渐变（顶部品牌色光晕 + 线性渐变），告别纯平 `#f4f6fb`
- **顶栏**：pill 容器包裹 Tab、品牌圆点加光晕环、更新时间胶囊化
- **阴影体系**：sm/md/lg 三级，卡片/面板 hover 带上浮 + 加深
- **滚动条**：自定义圆角细条，hover 变深
- **动画**：视图切换淡入上滑 `.26s`；模态框毛玻璃背景 + 入场动画 + 聚焦光环
- **细节**：圆角统一加大(14-16px)、间距宽松、字体渲染优化、Footer 分割线

---

> 数据源说明：
> - `hq_data.js` → HQ 排期主线
> - `fan_data.json` → 粉丝数据（IG/YTB/FB/X）
> - `REGIONAL_DATA` → 区域素材（内联于两个 HTML）
> - 两个 HTML 各自带内联 `FALLBACK_DATA` / `REGIONAL_DATA` 兜底块（分享版为自包含单文件）

---

### 2026-08-02 ｜ 每日产能常驻小条
- **功能**：topbar 下方新增「每日产能」小条，三个 Tab 均可见
- **内容**：复用海报×3 / 复用视频×1 / 原创视频×1(紫) / KV×1(紫) / AM跟进×1(绿) + 追加预算 $750/天
- **产能基线**：原产能（3复用海报+1复用视频/天）+ 追加 $750/天（1原创视频+1KV/天）+ AM跟进项目

---

## 当前数据基线（2026-08-02 快照）

### 粉丝数据（IG / YTB / FB / X）
- **IG**：1808（增长主因＝boost page like 付费投放，非自然内容增长）
- **YTB**：658（内容 / KOL 驱动，健康）
- **FB**：44（自然缓涨）
- **X**：24（停滞）
- **合计**：2534
- **目标**：10000（2026-08-20 上线前）
- 校准时间：2026-08-01 晚间截图

### HQ 排期关键节点（已写入 hq_data.js）
- 8/1：Creation Challenge 预注册创作挑战启动（3 周，玩家投稿→官方精选→二次传播）
- 8/3：导游 Bobby 带游 Rooklyn（城市探索预热）
- 8/4：幽灵族漫画（delay 至下周，日期待定）
- 8/5：电台周报第四期
- 8/6：经营玩法快剪（本地化优化中）
- 8/14：第二人生玩法向海报（内容方向及制作精度优化中）

### 区域素材（Week6：8/3–8/9，已写入 REGIONAL_DATA）
- 8/2（周日）：1 分钟网红混剪视频
- 8/3（周一）：KOC 官宣计划 ＋ KOC Creator Hub 注册一图流教程
- 8/3–8/7（周一~周五）：游戏内玩法展示素材混剪，逐日跟 HQ 主线
  - 8/3 落地 Rooklyn ／ 8/4 第二人生 ／ 8/5 怎么搞钱 ／ 8/6 从街头到老板 ／ 8/7 经营图鉴
- 8/3–8/9（每天）：Meme「我第一次来到了 Rooklyn」

### 进行中战役
- Creation Challenge：8/1–8/21，UGC 沉淀 ＋ 二次传播
- KOC 官宣 / Creator Hub：8/3 启动，为 Challenge 投稿蓄水

---

## 操作记录

### 2026-08-02 ｜ 建立版本记录机制（本操作）
- 初始化 git 仓库 ＋ 本 CHANGELOG.md
- 约定：今后每次增删改都 commit ＋ 追加记录，确保可追溯、删了能找回

### 2026-08-02 ｜ 回退四块结构升级
- 删除「本周作战总览 / 活动战役层 / 增长归因层 / 区域素材 Sourcing 状态管理」
- 保留：粉丝目标 10000、内联兜底数据同步（IG 1808 / YTB 658 / FB 44 / X 24）
- 恢复：区域素材周历为多周堆叠日历视图
- 校验：两文件 JS 语法通过、无残留、数据未丢失

### 2026-08-02 ｜ 区域素材周历支持拖拽改日期
- 功能：区域素材周历（Week5 / Week6）每张素材卡片可拖拽到其他日期格
- 实现：HTML5 drag & drop + 事件委托；移动记录存 localStorage `gangstar_regional_moves`（uid→目标日期，**增量**，不改源 REGIONAL_DATA）
- 特性：刷新不丢；拖回原日期自动清除该记录；支持跨周拖拽（按日期归属自动落到对应周）；主看板与分享版同步
- 校验：node 逻辑测试 PASS（8/3 KOC 拖到 8/7 生效），两文件 JS 语法 OK

### 2026-08-02 ｜ 拖拽改用 Pointer Events + 手柄按钮
- **原因**：HTML5 DnD 在部分浏览器环境下不触发 dragstart（用户反馈无法拖拽）
- **改动**：卡片右上角新增 ⠿ 拖拽手柄按钮（hover 变橙色）；放弃 HTML5 DnD API，改用 pointerdown/pointermove/pointerup 手动实现
- **效果**：手柄外区域不可拖拽（防误触）；ghost 跟随鼠标带旋转阴影效果；目标格橙色虚线高亮
- **校验**：无头浏览器测试 PASS（26 个手柄、ghost 跟随、drop 高亮、localStorage 写入、ghost 清理均正常）

### 2026-08-02 ｜ 南亚社群排期表也支持拖拽改日期
- **新增**：南亚排期表每张卡片右上角 ⠿ 手柄，按住可拖到任意日期格（跨周也行）
- **三类素材各自归属**：
  - HQ 公线 → `gangstar_sa_moves`（pid→目标日期）
  - 区域素材 → `gangstar_regional_moves`（**与区域素材周历共用同一份记录，两个视图始终一致**）
  - 自定义素材 → 直接改写 `gangstar_custom` 的日期归属
- **顺带修复**：区域素材 uid 之前是按「显示日期」生成的，导致二次拖拽产生残留记录、跨视图无法匹配 → 改为「原始日期＋序号」，uid 恒定唯一
- **校验**（主看板 ＋ 分享版均 ALL PASS）：两视图同步、HQ 落位、素材总数不变（113→113）、刷新持久化、二次拖拽无残留、拖回原位清除记录、区域周历回归正常、无 JS 报错、按钮不溢出

### 2026-08-02 ｜ 两表双向同步 + 全局撤销
- **核心问题**：在南亚社群排期删除/移动区域素材后，区域素材周历不变化（反之亦然）；撤销只覆盖南亚社群排期的 hide/cancel/edit，不含移动和区域视图
- **统一 ID 方案**：区域素材用 `range|date|type|title|idx`，自定义用 `cus|<id>`；两个表共用同一份 hidden/cancelled/edits/custom/moves
- **区域素材周历新增操作**：编辑(✎)、划线(划)、删除(×)、添加(+)——全部写入与南亚社群排期相同的 localStorage 键
- **自定义素材移动机制统一**：从「改写 gangstar_custom 日期」改为 `gangstar_regional_moves[cus|<id>]`，与源素材一致
- **全局撤销**：快照 7 个 localStorage key → 栈上限 50 条；顶部 ↩ 撤销按钮 + ⌘Z/Ctrl+Z；覆盖增删/移动/划线/编辑/粉丝目标
- **校验**（主看板+分享版 ALL PASS ×12）：删除同步、添加同步、撤销回退、撤销恢复删除项、编辑双向同步、拖拽移动双向同步

### 2026-08-02 ｜ 区域素材「已提制作需求」确认格子
- **功能**：每张区域素材卡片底部新增 ☐/✓ 确认格子（点击切换）
  - 含义：点一下 = 已提交该素材的制作需求（用户自己做的素材，需要跟踪提需求状态）
- **两表同步**：区域素材周历 + 南亚社群排期共用 `gangstar_req_submitted` 状态（按 canonical uid 键控）
- **HQ 公线不显示**：确认格子仅出现在区域素材上（HQ 公线无此格子）
- **全局撤销覆盖**：⌘Z 可撤销确认操作（`gangstar_req_submitted` 已加入 UNDO_KEYS 快照集）
- **刷新持久化**
- **校验**（主看板+分享版 ALL PASS ×11）：每张区域素材都有 req-box / 点击切换 on/off / localStorage 写入正确 / 南亚社群排期同步显示 / HQ 无 req-box / ⌘Z 撤销恢复 / 刷新持久化 / 无 JS 报错

### 2026-08-02 ｜ 确认格子视觉低调化（用户：不要这么明显）
- 去掉显眼绿色大药丸，改为浅灰小字 + 小方框
- 未确认：空心浅灰框 + “提需求”；确认后：淡绿小框内白✓ + 淡绿“已提需求”（无底色、无内边距）
- 仅视觉调整，同步/撤销/持久化逻辑不变
- git: 0176d8a

---

### 2026-08-02 ｜ 回退 UI v2（用户：太丑，不如刚刚）
- 用户反馈 UI v2（色系统一/柔化头部/毛玻璃顶栏/留白加大）"太丑了，还不如刚刚"，要求回退
- `git reset --hard a52c01a`，恢复至「原产能 vs 现产能」双行对照版本（即用户点过"很好"的版本）
- **结论 / 教训**：大刀阔斧的整体美化方向不被接受；今后美化要更克制，先给小样确认再落地，不要一次性改 18 项

### 2026-08-02 ｜ 粉丝数据更新（截图校准，晚）
- **IG**：2475 → **2568**（+93，boost page like 持续生效中）
- **YTB**：1000 → **1040**（+40，破千后继续增长）
- FB / X 无新截图，维持不变
- 三处同步：fan_data.json + 主看板内联 + 分享版内联

---

## 今后约定
1. 改数据：更新源头（fan_data.json / hq_data.js）→ 同步两个 HTML 内联块 → git commit → 追加本记录
2. 发社媒截图 = 直接更新粉丝数据（截图即更新，无需确认）
3. 不主动加管理类模块（作战 / 战役 / 归因 / 状态），保持简洁排期板
4. **UI 美化要克制**：先给小范围对比样（局部 CSS 改动截图）确认方向，再全面铺开；不要一次性大改

### 2026-08-02 ｜ 修正：$750 为「每月」非「每日」
- 用户更正：追加的 $750 预算是**每月**，不是每日
- 两文件产能条「追加 $750/天」→「追加 $750/月」；MEMORY.md 产能基线同步修正
- git: 待提交

### 2026-08-03 ｜ 新增「单日素材手动排序」
- **需求**：每个素材在单日内的排序也可手动控制（之前只能拖到别的日期）
- **交互**：按住卡片右上角拖拽手柄，在同一天单元格内上下拖动 → 实时橙色插入线提示落点 → 松手即重排
- **记忆**：两视图各自独立存 localStorage
  - 南亚社群排期：`gangstar_sa_order`（日期 → [pid 序列]，pid 即卡片 data-pid）
  - 区域素材周历：`gangstar_regional_order`（日期 → [uid 序列]，跨类型行完整序列）
- **撤销**：两键已加入全局撤销 UNDO_KEYS（⌘Z 覆盖）
- **拖到别的日期**：仍走原「改日期」逻辑，行为不变
- **测试**：无头浏览器双测 PASS（逻辑排序生效+跨重渲染保持；真实手柄拖拽→localStorage 写入一致；0 报错）
- 主看板 + 分享版同步

### 2026-08-03 ｜ 拖拽跟手优化
- 用户反馈拖拽「不跟手」，定位三处根因并修复：
  1. `pointermove` 每帧 `ghost.style.visibility` 来回切换 + `elementsFromPoint` 触发整页同步重排 → 主线程掉帧；改为依赖 ghost `pointer-events:none` 直接命中，去掉 reflow
  2. ghost 原用 `left/top` 定位（每次改值触发 layout）→ 改为 `transform: translate3d(...)` GPU 合成层，并加 `transition:none !important; will-change:transform` 杜绝继承过渡拖影
  3. 位置更新包 `requestAnimationFrame` 合并；`pointerdown` 加 `setPointerCapture(e.pointerId)` 兜底快速拖动丢事件；`cleanup` 取消挂起 rAF 防幽灵回调
- 验证：无头浏览器真实鼠标拖拽 → ghost 用 translate3d 跟手、同日内重排顺序正确写入 localStorage 且 DOM 同步、0 报错
- 主看板 + 分享版同步（两文件 JS 一致），已 commit
- 已知边界：拖到可视区之外的目标单元格因 `elementsFromPoint` 视口限制暂不命中；如需「拖到屏幕边缘自动滚动」可后续追加（本次未做，避免功能膨胀）

### 2026-08-03 ｜ 修复"编辑描述时弹窗误关"
- 现象：用户在编辑素材（标题/描述等）时，手一滑点到弹窗外灰色遮罩，弹窗立即关闭且编辑丢失（"直接返回到页面"）。
- 根因：三个弹窗（添加/编辑-南亚/编辑-区域）原 `bg.onclick=e=>{if(e.target===bg) bg.remove();}` —— 点遮罩即关、不保存。
- 修复：新增 `modalChangedGuard(bg)` —— 弹窗内任一输入（input/select/textarea）触发 `input`/`change` 即标记 `changed`；此后点遮罩不再关闭，必须点「取消」或「保存」；**完全未改动**时点遮罩仍可直接关（保留顺手关空弹窗）。
- 覆盖：添加素材、编辑南亚素材、编辑区域素材 三处弹窗
- 验证：无头浏览器 3 用例 PASS（编辑后点背景→仍开；未编辑点背景→关；改标题后点背景→仍开），0 报错；两文件同步、已 commit

### 2026-08-03 ｜ 加固弹窗守卫（修复"选中标题即关闭"）
- 现象：编辑素材标题时，鼠标刚选中（高亮）原标题文字，编辑窗口就秒关、内容全丢。
- 根因：上一版守卫仅凭 `input`/`change` 标记 `changed`；而浏览器对 `autofocus` 标题会默认全选文字，"点标题/选中文字"本身不触发 input/change → `changed` 仍为 false；一旦某次落点被判定为点到背景遮罩（如拖选松手略移出输入框、聚焦态点击歧义），即 `e.target===bg` 成立而 `changed=false` → 弹窗秒关。
- 修复：守卫升级 —— 任一输入框 `focus`/`pointerdown`/`mousedown`/`input`/`change` 任一发生即标记 `engaged`（即"已介入"），此后点背景不再能关（只能取消/保存）；并加 `downOnBg` 判定：仅当点击手势确实从背景遮罩起始（排除"框内拖选松手落背景"）才视为一次有效背景点击；只有「完全没碰过」且手势从背景开始，点遮罩才可关。
- 覆盖：添加素材 / 编辑南亚素材 / 编辑区域素材 三处弹窗（统一 `modalChangedGuard`）
- 验证：无头浏览器 4 用例全 PASS（点标题后点背景→仍开；未碰过点背景→关；输入后点背景→仍开；框内拖选松手到背景→仍开），0 报错；两文件同步、已 commit
